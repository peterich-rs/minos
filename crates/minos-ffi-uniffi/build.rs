fn main() {
    // UniFFI proc-macro mode (no UDL file). Real setup lands in plan 02.
    // This empty build.rs exists so the crate has a predictable build-phase
    // hook for later additions (e.g., linker flags for staticlib on iOS).
}
