//! Mobile envelope client tests (Host Link era).
//!
//! QR `pair_with_qr_json` e2e against backend pairing codes was removed with
//! the QR stack. Re-add Host Link–aware mobile session coverage when mobile
//! consumes `GET /v1/hosts` as the bind/list surface.

// Intentionally empty after QR pairing deletion (latest-only).
