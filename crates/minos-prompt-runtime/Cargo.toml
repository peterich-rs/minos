[package]
name = "minos-prompt-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Prompt package compiler: SessionContext → CompiledPromptBundle with deterministic digests and runtime delivery surfaces."

[dependencies]
sha2 = { workspace = true }
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
pretty_assertions = { workspace = true }

[lints]
workspace = true
